- test lookups during joins
- test inserts during joins

- determine when to run stabilize at runtime
- server selection
- implement nodeID identification
- smart implementation of location table
- recursive lookup
- use refcnted locations.

- fix assert when nodeID is not there (dispatch)
- replacefinger shouldn't replace first by finding succ of start (ipv first)
